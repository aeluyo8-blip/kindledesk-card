interface:
  display_name: "KindleDesk Card"
  short_description: "为已配置的 Kindle 副屏安全预览、推送卡片并检查运行连接"
  default_prompt: "Use $kindledesk-card to preview a KindleDesk card and push it only after I explicitly confirm."
